[project]
name = "failsafe-validation-tools"
version = "0.1.0"
description = "Validation MCP server for Failsafe Foundry"
requires-python = ">=3.11"
dependencies = [
  "fastmcp",
  "pytest"
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"